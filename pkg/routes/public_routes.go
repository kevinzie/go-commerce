package routes

import (
	"github.com/gofiber/fiber/v2"
)

func PublicRoutes(a *fiber.App) {

	//route := a.Group("/api/v1")
	//auth := route.Group("/auth")
	//auth.Get("/login", controllers.GetUsers)
}
